<article>
    <a href="/dashboard">Return to Dashboard</a>
    <h2> Edit Post</h2>
    <form class="editPost-form">
        <div>
            <input name="postTitle" type="text" value="{{post.title}}" />
        </div>
        <div class="editPost-info">
            Created on {{format_date post.created_at}} Upvotes: {{post.likes_count}} {{format_plural "Like" post.likes_count}}
            <a href="/post/{{post.id}}">{{post.comments.length}} {{format_plural "comment" post.comments.length}}</a>
        </div>
        <div class="edit-btn">
        <button type="submit" class="save-btn"> Save Post</button>
        <button type="button" class="delete-btn">Delete Post</button>
        </div>
    </form> 
</article>

<form class="commentForm">
    <div>
        <textarea name="commentBody"></textarea>
    </div>
    <div>
        <button type="submit">Add a Comment</button>
    </div>
</form>

{{> comments post.comments}}

<script src="/javascript/editPost.js"></script>
<script src="/javascript/deletePost.js"></script>
<script src="/javascript/comment.js"></script>